# Force install strategy in CI to avoid npm ci issues with workspace/lockfile
npm_config_install_strategy=shallow

# Disable npm workspaces in Railway builds
workspaces=false
include-workspace-root=false
