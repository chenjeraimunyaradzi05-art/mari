# The root package.json is not a deployable package (its build and start
# scripts refuse to run on purpose). These settings stop a host that builds
# from the repository root from treating it as a workspace and installing the
# whole tree; each app installs from its own directory.
npm_config_install_strategy=shallow
workspaces=false
include-workspace-root=false
