{
  "name": "athena-monorepo-root",
  "version": "1.0.0",
  "description": "ATHENA Platform Monorepo - DO NOT DEPLOY FROM ROOT",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "echo 'ERROR: Do not build from root. Deploy from athena-platform/server or athena-platform/client' && exit 1",
    "start": "echo 'ERROR: Do not start from root. Deploy from athena-platform/server or athena-platform/client' && exit 1",
    "dev:api": "cd athena-platform/server && npm run dev",
    "dev:web": "cd athena-platform/client && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "db:seed": "cd athena-platform/server && npm run db:seed",
    "db:migrate": "cd athena-platform/server && npx prisma migrate deploy",
    "db:generate": "cd athena-platform/server && npx prisma generate"
  }
}