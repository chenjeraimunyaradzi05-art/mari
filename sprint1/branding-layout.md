# Sprint 1 — Branding & Layout

Estimate: 8h

Goal
- Implement global branding and core layout components used across the site.

Acceptance criteria
- Visual QA sign-off on header, footer, colors, and responsive layout.
- Layout components available for reuse (e.g., `SiteHeader`, `SiteFooter`, `MainLayout`).

Checklist
- [ ] Add brand color variables to Tailwind config
- [ ] Implement `SiteHeader` (desktop + mobile)
- [ ] Implement `SiteFooter`
- [ ] Create global layout wrapper and apply to `app/layout.tsx`
- [ ] Add a static Storybook or `app/branding` demo page

Notes
- Branch: `sprint1/branding-layout`
