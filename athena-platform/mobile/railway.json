{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "Mobile apps are NOT deployed on Railway. Use EAS Build instead.",
  "_instructions": [
    "This file exists only for documentation purposes.",
    "To build your mobile app:",
    "1. Install EAS CLI: npm install -g eas-cli",
    "2. Login to Expo: eas login",
    "3. Build: eas build --platform all --profile preview",
    "4. Or use the GitHub Actions workflow: .github/workflows/mobile-build.yml"
  ],
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo 'Mobile apps should be built with EAS Build, not Railway. See README.md'"
  }
}
