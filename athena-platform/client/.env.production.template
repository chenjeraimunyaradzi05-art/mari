# ===========================================
# ATHENA CLIENT - PRODUCTION ENVIRONMENT
# ===========================================
# INSTRUCTIONS:
# 1. Add these to your Netlify/Vercel Environment Variables
# 2. All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 3. Never include sensitive secrets here
# ===========================================

# ───────────────────────────────────────────
# API CONFIGURATION
# ───────────────────────────────────────────
# Your deployed backend API URL
NEXT_PUBLIC_API_URL=https://api.your-domain.com

# This app's URL (used for OAuth callbacks, etc.)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# ───────────────────────────────────────────
# FEATURE FLAGS
# ───────────────────────────────────────────
NEXT_PUBLIC_ENABLE_AI_FEATURES=true
NEXT_PUBLIC_ENABLE_SOCIAL_LOGIN=false
NEXT_PUBLIC_ENABLE_VIDEO_UPLOAD=true
NEXT_PUBLIC_ENABLE_PAYMENTS=true

# ───────────────────────────────────────────
# THIRD-PARTY SERVICES (Public Keys Only)
# ───────────────────────────────────────────
# Stripe publishable key (pk_live_ for production)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Google Analytics (optional)
NEXT_PUBLIC_GA_MEASUREMENT_ID=your_ga_measurement_id_here

# ───────────────────────────────────────────
# REGIONAL SETTINGS
# ───────────────────────────────────────────
NEXT_PUBLIC_DEFAULT_LOCALE=en-GB
NEXT_PUBLIC_SUPPORTED_LOCALES=en-GB,en-US,es-ES

# ───────────────────────────────────────────
# BUILD-TIME VARIABLES (Not exposed to browser)
# ───────────────────────────────────────────
# Sentry for error tracking (optional)
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
