# Nixpacks configuration for ATHENA Web Client
# Railway Root Directory: athena-platform/client

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
