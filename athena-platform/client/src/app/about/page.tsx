import HomepageLanding from '@/components/home/HomepageLanding';

// The product tour that used to be the homepage. `/` is now the Instagram-style
// feed, so this keeps the marketing narrative reachable instead of deleting it.
export default function AboutPage() {
  return <HomepageLanding />;
}
