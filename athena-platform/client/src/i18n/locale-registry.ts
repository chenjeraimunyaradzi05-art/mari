import registry from '../../i18n.registry.js';

export const locales = registry.locales as string[];
export const defaultLocale = registry.defaultLocale as string;
export const rtlLocales = registry.rtlLocales as string[];
