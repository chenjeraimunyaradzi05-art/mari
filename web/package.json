{
  "name": "athena-web",
  "version": "1.0.0",
  "description": "ATHENA Platform Web Client",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "postinstall": "cd athena-platform/client && npm install --legacy-peer-deps",
    "build": "cd athena-platform/client && npm run build",
    "start": "cd athena-platform/client && npm start",
    "dev": "cd athena-platform/client && npm run dev",
    "lint": "cd athena-platform/client && npm run lint",
    "test": "cd athena-platform/client && npm test"
  },
  "dependencies": {}
}
